import { StrtolowerPipe } from './strtolower.pipe';

describe('StrtolowerPipe', () => {
  it('create an instance', () => {
    const pipe = new StrtolowerPipe();
    expect(pipe).toBeTruthy();
  });
});
